# Install from Debian Repo. (The easiest way):

- sudo apt update
- sudo apt install hashcat -y

## Install Required Dependencies:
- sudo apt update
- sudo apt install git build-essentials -y

## Testing Hashcat:
- hashcat --help
